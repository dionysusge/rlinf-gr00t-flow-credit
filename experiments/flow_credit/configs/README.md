# Experiment configurations

The runnable Hydra configurations are maintained under:

`examples/embodiment/config/`

This directory is reserved for future generated experiment matrices and
configuration documentation. Do not duplicate active YAML configurations here.
